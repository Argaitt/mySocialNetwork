import React from "react";
import preloaderImg from '../../assets/images/preloader.gif'

let preloader = (props) => {
    return <div>
        <img src={preloaderImg} alt="not found"/>
    </div>
}

export default preloader